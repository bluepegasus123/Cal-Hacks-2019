class Course:
	""" Class for defining Course objects """
	def __init__(self, name, instructor, section, time):
		self.name = name
		self.instructor = instructor
		self.section = section
		self.time = time
	